import { ShoppingCartDirective } from './shopping-cart.directive';

describe('ShoppingCartDirective', () => {
  it('should create an instance', () => {
    const directive = new ShoppingCartDirective();
    expect(directive).toBeTruthy();
  });
});
